# Core dependencies with known compatible versions
fastapi==0.95.0
uvicorn[standard]==0.21.1
python-multipart==0.0.6
python-dotenv==0.21.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# AI/ML
numpy==1.23.5  # Pinned to a version compatible with sentence-transformers
scikit-learn==1.1.3  # Pinned to a version compatible with sentence-transformers
sentence-transformers==2.2.2
faiss-cpu==1.7.4
anthropic==0.7.5

# HTTP & API
httpx==0.23.3
requests==2.28.2

# Templates
jinja2==3.1.2

# File handling
aiofiles==22.1.0  # Slightly older version for better compatibility

# Payments
stripe==5.4.0

# Pydantic and Starlette (pinned to versions compatible with FastAPI 0.95.0)
pydantic==1.10.7
starlette==0.26.0

# Additional dependencies that might be needed
anyio==3.6.2
click==8.1.3
h11==0.14.0
sniffio==1.3.0
typing-extensions==4.5.0
